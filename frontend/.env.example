# Frontend Environment Variables
# If deploying full-stack in a unified Vercel deployment, leave VITE_API_BASE_URL empty or unset (defaults to /api)
# If deploying frontend to Vercel and backend elsewhere (Render, Railway, VPS), set your backend API URL below:
# VITE_API_BASE_URL=https://your-backend-api.onrender.com/api
